<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Profile Section -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_user"
            android:icon="@drawable/ic_user"
            android:title="User"/>

        <item
            android:id="@+id/nav_progress"
            android:icon="@drawable/ic_progress"
            android:title="Progress"/>

        <item
            android:id="@+id/nav_user_setup"
            android:icon="@drawable/ic_logout"
            android:title="User Setup"/>
    </group>

    <!-- Workout Section -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_workout_plans"
            android:icon="@drawable/ic_workout"
            android:title="Workout Plans"/>

        <item
            android:id="@+id/nav_create_workout"
            android:icon="@drawable/ic_create_workout"
            android:title="Create Workout"/>
    </group>

    <!-- Settings -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_settings"
            android:title="Settings"/>
    </group>

    <!-- Dark Mode Toggle -->
    <item
        android:id="@+id/nav_dark_mode"
        android:title=""
        android:actionLayout="@layout/activity_dark_mode_toggle"/>
</menu>
